// Stratora release version — update this for each new release
export const RELEASE_VERSION = "2.1.0";
export const MSI_DOWNLOAD_URL = `https://github.com/Stratora-Platforms/stratora/releases/download/v${RELEASE_VERSION}/Stratora-Server-${RELEASE_VERSION}.msi`;
